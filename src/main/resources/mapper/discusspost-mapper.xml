<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.dao.DiscussPostMapper">
    <sql id="selectField">
        id,user_id,title,content,type,status,create_time,comment_count,score
    </sql>
    <select id="selectPosts" resultType="DiscussPost">
        select <include refid="selectField"></include>
        from discuss_post
        where status!=2/*没被拉黑才能显示*/
        <if test="userId!=0">and user_id=#{userId}</if>/*输入userId了才拼*/
        order by type desc,create_time desc/*精华先显示，同类型就按日期最新的显示*/
        limit #{offset},#{limit}/*分页*/

    </select>
    <select id="selectPostRows" resultType="java.lang.Integer">
        select count(id) from discuss_post
        where status!=2
        <if test="userId!=0">and user_id=#{userId}</if>
    </select>
</mapper>